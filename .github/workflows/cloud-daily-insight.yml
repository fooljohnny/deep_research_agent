name: Cloud Daily Insight

on:
  schedule:
    # 北京时间凌晨 5:00 = UTC 21:00 (前一天)
    - cron: "0 21 * * *"
  workflow_dispatch:

permissions:
  contents: write

defaults:
  run:
    working-directory: cloud-daily-insight

jobs:
  insight:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: cloud-daily-insight/requirements.txt

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run pipeline
        working-directory: cloud-daily-insight/scripts
        env:
          LLM_PROVIDER: ${{ vars.LLM_PROVIDER || 'groq' }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_MODEL: ${{ vars.LLM_MODEL || 'groq/compound' }}
          # Optional; required for LLM_PROVIDER=custom. Use "deepseek" provider for DeepSeek without this.
          LLM_BASE_URL: ${{ vars.LLM_BASE_URL }}
          LLM_JSON_RESPONSE_FORMAT: ${{ vars.LLM_JSON_RESPONSE_FORMAT }}
          STAGE_DELAY_SEC: ${{ vars.STAGE_DELAY_SEC || '65' }}
          RETRY_DELAY_SEC: ${{ vars.RETRY_DELAY_SEC || '65' }}
          BATCH_DELAY_SEC: ${{ vars.BATCH_DELAY_SEC || '65' }}
        run: python main.py

      - name: Commit & push new post
        working-directory: .
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add cloud-daily-insight/content/ cloud-daily-insight/data/ cloud-daily-insight/logs/
          if ! git diff --cached --quiet; then
            git commit -m "cloud-insight: $(date -u +%Y-%m-%d) daily post"
            git push
          else
            echo "No changes to commit."
          fi
